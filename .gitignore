# Système
.DS_Store
*.log
*.tmp

# Python (si stack Python retenue)
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/

# Rust (si stack Rust retenue)
/target/

# Node (si stack Node retenue)
node_modules/

# Données runtime du BBS (à versionner séparément si besoin)
/data/
*.db

# Artefacts client Oric (regénérés par client/build.sh + client/build-disk.sh ;
# distribués via les Releases GitHub, pas versionnés dans le dépôt)
client/term.bin
client/term.tap
client/term-boot.dsk

# Config de déploiement réelle (infra prod) — utiliser les *.conf.example
deploy/deploy.conf
deploy/profiles/**/*.conf

# Binaires (à la racine uniquement — ne pas ignorer le dossier cmd/forge/)
/bbsd
/forge

# Outillage assistant IA (non distribué — local uniquement)
CLAUDE.md
.claude/
