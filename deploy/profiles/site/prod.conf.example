# Profil PRODUCTION — serveur distant (SSH). Copier en prod.conf et renseigner.
# prod.conf est gitignoré (ne JAMAIS versionner l'infra réelle).
HOST=10.0.0.1
USER=root
PORT=22
CONTENT_PATH=/etc/bbsoric/site.json
SERVICE=bbsoric
RELOAD=none
